%read in image
train = csvread('data/train.csv',1,0);

%build a random image specified by "image"
m = zeros(28,28);
image = 4;

for i = 1:28,
    for j = 1:28,
        m(i, j) = train(image,(i-1)*28+j+1);
    end
end

figure(1)
imagesc(m)

%% take the image to b/w
m = im2bw(m, 0.5);
imagesc(m)