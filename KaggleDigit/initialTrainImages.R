#Import the dataset
train <- read.csv("C:/GitHub/PopTip/KaggleDigit/data/train.csv", header=TRUE)
test <- read.csv("C:/GitHub/PopTip/KaggleDigit/data/test.csv", header=TRUE)

#look at images
#create an image:
m <- matrix(28,28,0);
for (i in 1:28)
  m()

#preprocess the images

#break data into subsets